import { ChartData } from '@/types';
import { 
  pieChartData, 
  barChartData, 
  industryBarData, 
  donutChartData, 
  lineChartData 
} from '@/data/appData';

// All chart data is now imported from the centralized appData.json file
// This file is kept for backwards compatibility
// In the future, import directly from '@/data/appData'
