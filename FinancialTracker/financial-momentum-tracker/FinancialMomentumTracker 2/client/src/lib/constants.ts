import { PortfolioType } from '@/types';
import { MASTERCARD_COLORS, OPPORTUNITY_SCORES, TRANSACTION_TYPES } from '@/data/appData';

// All constants are now imported from the centralized appData.json file
// This file is kept for backwards compatibility
// In the future, import directly from '@/data/appData'
